import type ControlField from "./field/ControlField.js";
import type AddressField from "./field/AddressField.js";
import type IFragment from "./IFragment.js";

export default abstract class GetRequestFrame {
    // readonly START_FLAG = 0X68;
    // readonly END_FLAG = 0X16;
    // readonly len: number;
    // readonly controlField: ControlField;
    // readonly addressField: AddressField;
    // readonly hcs: number;
    // readonly apdu: IFragment;
    // readonly fcs: number;
    //
    // constructor(addressField: AddressField, controlField: ControlField, apdu: IFragment) {
    //
    // }
}